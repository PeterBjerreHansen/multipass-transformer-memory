# Smoke control

The short CPU vanilla run checks that training, the active 2048-token data
artifact, and checkpoint plumbing work end to end. Raw output is written under
`results/vanilla/` and ignored.
