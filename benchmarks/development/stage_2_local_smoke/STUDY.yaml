name: stage_2_local_smoke
status: planned
question: Do the wired feedback models remain stable when the backbone is unfrozen?
arms:
  - {id: recirculation_adaptive_smoke, config: recirculation_adaptive_smoke.yaml}
  - {id: bank_dense_smoke, config: bank_dense_smoke.yaml}
  - {id: bank_periodic32_smoke, config: bank_periodic32_smoke.yaml}
  - {id: bank_memory_token32_smoke, config: bank_memory_token32_smoke.yaml}
  - {id: hybrid_recirculation_smoke, config: hybrid_recirculation_smoke.yaml}
comparisons: []
