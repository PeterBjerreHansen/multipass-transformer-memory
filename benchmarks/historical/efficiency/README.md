# Superseded efficiency suites

These suites and retained JSON measurements describe the former 2,048-token
Stage-5 architecture set, its attention controls, Memory Attention write
scaling, and its old CUDA batching qualification. They are not execution inputs
for the current 1,024-token paper contract.

Current forward-policy and general hardware suites remain in
`benchmarks/efficiency/suites/`.
