# Renamed inference contract

This generic document name is retained only so old links do not break. The
active contract is [Exact K-pass and Live Feedback inference](FEEDBACK_INFERENCE.md).
“Recurrent” remains valid for mechanism-specific recurrent architectures, but
is no longer the name of the generic one-stream feedback execution mode.
