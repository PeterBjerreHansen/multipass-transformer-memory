# Free-generation capability suite. K applies only to prompt prefill; each
# generated token advances the selected decode mechanism exactly once.
kind: generation
tasks:
  - {name: gsm8k, num_fewshot: 5}
  - {name: hendrycks_math, num_fewshot: 4}
